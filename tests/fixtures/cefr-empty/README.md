# Deliberately empty: the "no wordlist installed" state, which is what a fresh
# clone has. Tests that must exercise the grammar-only and tiebreak paths point
# here so they cannot be perturbed by cefr/ gaining entries.
